services:
  klao-console:
    container_name: klao-console
    image: klao-console:latest
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

services:
  sails-console:
    container_name: sails-console
    image: sails-console:latest
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

/**
 * Lucide icon names ↔ components for workflow action styling.
 * Names align with the values produced by shared `defaultActionStyle()`.
 */
import {
  Circle, CheckCircle2, XCircle, RefreshCw, Info, CornerUpLeft, ThumbsUp, ThumbsDown, Clock, AlertTriangle,
} from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

export const ICON_MAP: Record<string, LucideIcon> = {
  Circle,
  CheckCircle2,
  XCircle,
  RefreshCw,
  Info,
  CornerUpLeft,
  ThumbsUp,
  ThumbsDown,
  Clock,
  AlertTriangle,
};
